# Demo mode: `npm run dev -- --mode demo` overrides .env with these values,
# pointing the app at the local mock API (npm run mock).
VITE_SUPABASE_URL=http://localhost:54321
VITE_SUPABASE_ANON_KEY=local-demo
